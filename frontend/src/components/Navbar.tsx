import { Flex } from '@chakra-ui/react';
import { Link } from 'react-router-dom';

const Navbar = () => {
    return (
        <Flex>
            Pomo
            Login
        </Flex>


    );
}

export default Navbar;